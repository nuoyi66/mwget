Mwget
===========

Mwget is a "MultiLine" wget for all POSTX System.


Installation
------------

As usual for GNU packages:
    $ apt instal gcc g++ pkg-config make libssl-dev 
    $ apt upgrade intltool
    $ ./configure --prefix=[[PREFIX]]     where [[PREFIX]] is e.g. $HOME/local
    $ make
    $ make install
